<% content_for :header do %>
	<%= javascript_include_tag :defaults %>
	<%= javascript_include_tag :fckeditor %>
<% end %>

<%= render :partial => "base_page/templates/top_menu" %>
<h1>Редактировать наши услуги.</h1>
<% form_for(@services_page, :url => {:controller => :base_page, :action => :do_edit_services_page, :id => @services_page.id}) do |f| %>
  <p>
    Ссылка на страницу:
    <%= link_to "#{url_for( :only_path => false, :controller => :main, :action => :services )}", :controller => :main, :action => :services %>
  </p>
  <p>
    <%= f.label :title, "Заголовок" %><br />
    <%= f.text_field :title, :style => "width: 100%" %>
  </p>
    <p>
        <%= f.label :meta_keywords, "Мета слова(meta keywords):" %>
    </p>
    <p>
      <%= f.text_area :meta_keywords, :rows => 5, :style => "width: 100%" %>
    </p>
    <p>
        <%= f.label :meta_description, "Мета описание(meta description):" %>
    </p>
    <p>
      <%= f.text_area :meta_description, :rows => 5, :style => "width: 100%" %>
    </p>
  <div>
    <%= f.label :comment, "Дополнительная информация(в самом низу страницы)" %><br />
    <%= fckeditor_textarea("services_page", "body", :toolbarSet => 'Simple', :width => '100%', :height => '400px') %>
  </div>
  <%= submit_tag("Обновить") %>
<% end %>

